{{#if hasBlock}}
  {{yield (hash change=(action 'change') isSelected=(readonly isSelected) row=row body=true)}}
{{else}}
  {{input type='checkbox' change=(action 'change' value='target.checked') checked=(readonly isSelected)}}
{{/if}}