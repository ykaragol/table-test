{{#if hasBlock}}
  {{yield (hash change=(action 'change') isSelected=(readonly isSelected) footer=true)}}
{{else}}
  {{input type='checkbox' change=(action 'change' value='target.checked') checked=(readonly isSelected)}}
{{/if}}